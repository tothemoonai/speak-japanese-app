# 日语口语练习App - 测试文档

## 📋 目录

1. [测试概述](#测试概述)
2. [测试环境准备](#测试环境准备)
3. [单元测试](#单元测试)
4. [集成测试](#集成测试)
5. [端到端测试](#端到端测试)
6. [手动测试用例](#手动测试用例)
7. [性能测试](#性能测试)
8. [安全测试](#安全测试)
9. [测试报告](#测试报告)

---

## 测试概述

### 测试目标

确保应用的质量和稳定性，包括：
- ✅ 功能正确性
- ✅ 用户体验
- ✅ 性能表现
- ✅ 安全性
- ✅ 兼容性

### 测试范围

| 模块 | 测试类型 | 优先级 |
|------|---------|--------|
| 认证模块 | 单元测试 + E2E | P0 |
| 课程模块 | 单元测试 + E2E | P0 |
| 练习模块 | 单元测试 + 集成测试 + E2E | P0 |
| 报告模块 | 集成测试 + E2E | P1 |
| 分享模块 | 集成测试 + E2E | P1 |
| UI组件 | 单元测试 | P2 |

### 测试工具栈

- **单元测试**: Jest + React Testing Library
- **E2E测试**: Playwright
- **API测试**: Supertest
- **性能测试**: Lighthouse
- **代码覆盖率**: Istanbul (Jest内置)

---

## 测试环境准备

### 步骤1: 安装测试依赖

```bash
# 安装测试相关的依赖
npm install --save-dev @testing-library/react @testing-library/jest-dom \
  @testing-library/user-event jest jest-environment-jsdom \
  @playwright/test

# 安装TypeScript类型
npm install --save-dev @types/jest @types/testing-library__jest-dom
```

### 步骤2: 配置Jest

创建 `jest.config.js`:

```javascript
const nextJest = require('next/jest')

const createJestConfig = nextJest({
  // Provide the path to your Next.js app to load next.config.js and .env files in your test environment.
  dir: './',
})

// Add any custom config to be passed to Jest
const customJestConfig = {
  setupFilesAfterEnv: ['<rootDir>/jest.setup.js'],
  testEnvironment: 'jest-environment-jsdom',
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1',
  },
  collectCoverageFrom: [
    'src/**/*.{js,jsx,ts,tsx}',
    '!src/**/*.d.ts',
    '!src/**/*.stories.tsx',
    '!src/**/__tests__',
  ],
}

// createJestConfig is exported this way to ensure that next/jest can load the Next.js config which is async
module.exports = createJestConfig(customJestConfig)
```

### 步骤3: 配置测试设置文件

创建 `jest.setup.js`:

```javascript
// Learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom'

// Mock Supabase
global.Supabase = {
  createClient: jest.fn(() => ({
    auth: {
      signUp: jest.fn(),
      signInWithPassword: jest.fn(),
      signOut: jest.fn(),
      getUser: jest.fn(),
    },
    from: jest.fn(() => ({
      select: jest.fn(),
      insert: jest.fn(),
      update: jest.fn(),
      delete: jest.fn(),
    })),
  })),
}

// Mock Web Speech API
global.SpeechSynthesisUtterance = class SpeechSynthesisUtterance {}
global.speechSynthesis = {
  speak: jest.fn(),
  cancel: jest.fn(),
  pause: jest.fn(),
  resume: jest.fn(),
}

// Mock MediaRecorder
global.MediaRecorder = class {
  constructor() {
    this.state = 'inactive'
  }
  start() {
    this.state = 'recording'
    this.onstart?.()
  }
  stop() {
    this.state = 'inactive'
    this.onstop?.()
  }
  addEventListener() {}
  removeEventListener() {}
}
```

### 步骤4: 配置测试环境变量

创建 `.env.test`:

```env
# 测试环境配置
NEXT_PUBLIC_SUPABASE_URL=https://test-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=test-anon-key

# AI服务测试（使用测试密钥或mock）
OPENAI_API_KEY=test-key-openai
ANTHROPIC_API_KEY=test-key-anthropic
ZHIPU_API_KEY=test-key-zhipu

# 注意：实际测试时应该mock这些API调用，避免消耗真实额度
```

### 步骤5: 配置Playwright

创建 `playwright.config.ts`:

```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },

  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'firefox',
      use: { ...devices['Desktop Firefox'] },
    },
    {
      name: 'webkit',
      use: { ...devices['Desktop Safari'] },
    },
    {
      name: 'Mobile Chrome',
      use: { ...devices['Pixel 5'] },
    },
    {
      name: 'Mobile Safari',
      use: { ...devices['iPhone 12'] },
    },
  ],

  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
});
```

### 步骤5: 更新package.json

添加测试脚本：

```json
{
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  }
}
```

---

## 单元测试

### 测试工具组件

#### 1. 测试Button组件

创建 `src/components/ui/__tests__/button.test.tsx`:

```typescript
import React from 'react';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Button } from '../button';

describe('Button Component', () => {
  it('renders correctly with text', () => {
    render(<Button>Click me</Button>);
    expect(screen.getByRole('button')).toHaveTextContent('Click me');
  });

  it('applies default variant styles', () => {
    render(<Button>Default Button</Button>);
    const button = screen.getByRole('button');
    expect(button).toHaveClass('bg-primary');
  });

  it('applies destructive variant styles', () => {
    render(<Button variant="destructive">Delete</Button>);
    const button = screen.getByRole('button');
    expect(button).toHaveClass('bg-destructive');
  });

  it('applies size styles', () => {
    render(<Button size="lg">Large Button</Button>);
    const button = screen.getByRole('button');
    expect(button).toHaveClass('h-11');
  });

  it('handles click events', async () => {
    const handleClick = jest.fn();
    const user = userEvent.setup();

    render(<Button onClick={handleClick}>Click me</Button>);

    await user.click(screen.getByRole('button'));

    expect(handleClick).toHaveBeenCalledTimes(1);
  });

  it('is disabled when isLoading is true', () => {
    render(<Button disabled>Disabled</Button>);
    const button = screen.getByRole('button');
    expect(button).toBeDisabled();
  });
});
```

#### 2. 测试Input组件

创建 `src/components/ui/__tests__/input.test.tsx`:

```typescript
import React from 'react';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Input } from '../input';

describe('Input Component', () => {
  it('renders correctly', () => {
    render(<Input placeholder="Enter text" />);
    expect(screen.getByPlaceholderText('Enter text')).toBeInTheDocument();
  });

  it('accepts user input', async () => {
    const user = userEvent.setup();
    render(<Input data-testid="test-input" />);

    const input = screen.getByTestId('test-input');
    await user.type(input, 'Hello World');

    expect(input).toHaveValue('Hello World');
  });

  it('applies custom className', () => {
    render(<Input className="custom-class" />);
    const input = screen.getByRole('textbox');
    expect(input).toHaveClass('custom-class');
  });

  it('respects disabled state', () => {
    render(<Input disabled />);
    const input = screen.getByRole('textbox');
    expect(input).toBeDisabled();
  });
});
```

### 测试工具函数

创建 `src/lib/utils/__tests__/cn.test.ts`:

```typescript
import { cn } from '../cn';

describe('cn utility function', () => {
  it('merges class names correctly', () => {
    expect(cn('btn', 'btn-primary')).toBe('btn btn-primary');
  });

  it('handles conditional classes', () => {
    expect(cn('btn', false && 'hidden', true && 'visible')).toBe('btn visible');
  });

  it('handles undefined and null', () => {
    expect(cn('btn', undefined, null)).toBe('btn');
  });

  it('deduplicates tailwind classes', () => {
    expect(cn('p-4 p-4')).toBe('p-4');
  });
});
```

### 测试Hooks

#### 测试useAuthStore

创建 `src/store/__tests__/authStore.test.ts`:

```typescript
import { renderHook, act, waitFor } from '@testing-library/react';
import { useAuthStore } from '../authStore';

// Mock the auth service
jest.mock('@/services/supabase/auth.service', () => ({
  authService: {
    login: jest.fn(),
    register: jest.fn(),
    logout: jest.fn(),
  },
}));

describe('useAuthStore', () => {
  beforeEach(() => {
    // Reset store before each test
    useAuthStore.setState({
      user: null,
      isAuthenticated: false,
      isLoading: false,
    });
  });

  it('has correct initial state', () => {
    const { result } = renderHook(() => useAuthStore());

    expect(result.current.user).toBeNull();
    expect(result.current.isAuthenticated).toBe(false);
    expect(result.current.isLoading).toBe(false);
  });

  it('logs in successfully', async () => {
    const { authService } = require('@/services/supabase/auth.service');
    const mockUser = { id: '123', email: 'test@test.com' };

    (authService.login as jest.Mock).mockResolvedValue({
      user: mockUser,
      error: null,
    });

    const { result } = renderHook(() => useAuthStore());

    await act(async () => {
      await result.current.login('test@test.com', 'password');
    });

    expect(result.current.user).toEqual(mockUser);
    expect(result.current.isAuthenticated).toBe(true);
  });

  it('handles login error', async () => {
    const { authService } = require('@/services/supabase/auth.service');

    (authService.login as jest.Mock).mockResolvedValue({
      user: null,
      error: { message: 'Invalid credentials' },
    });

    const { result } = renderHook(() => useAuthStore());

    await expect(async () => {
      await act(async () => {
        await result.current.login('test@test.com', 'wrong');
      });
    }).rejects.toThrow('Invalid credentials');

    expect(result.current.user).toBeNull();
    expect(result.current.isAuthenticated).toBe(false);
  });

  it('logs out successfully', async () => {
    const { authService } = require('@/services/supabase/auth.service');

    // 先设置登录状态
    useAuthStore.setState({
      user: { id: '123', email: 'test@test.com' },
      isAuthenticated: true,
      isLoading: false,
    });

    (authService.logout as jest.Mock).mockResolvedValue({
      error: null,
    });

    const { result } = renderHook(() => useAuthStore());

    await act(async () => {
      await result.current.logout();
    });

    expect(result.current.user).toBeNull();
    expect(result.current.isAuthenticated).toBe(false);
  });
});
```

### 运行单元测试

```bash
# 运行所有测试
npm test

# 监听模式（自动重新运行）
npm run test:watch

# 生成覆盖率报告
npm run test:coverage
```

---

## 集成测试

### 测试API路由

#### 测试登录API

创建 `tests/api/auth/login.test.ts`:

```typescript
import { createMocks } from 'node-mocks-http';
import { POST } from '@/app/api/auth/login/route';

jest.mock('@/services/supabase/auth.service');

describe('/api/auth/login', () => {
  it('returns user data on successful login', async () => {
    const mockUser = {
      id: '123',
      email: 'test@test.com',
      user_metadata: { nickname: 'Test User' },
    };

    const { authService } = require('@/services/supabase/auth.service');
    (authService.login as jest.Mock).mockResolvedValue({
      user: mockUser,
      error: null,
    });

    const request = createMocks({
      method: 'POST',
      body: {
        email: 'test@test.com',
        password: 'password123',
      },
    });

    const response = await POST(request);
    const data = await response.json();

    expect(response.status).toBe(200);
    expect(data.user.email).toBe('test@test.com');
  });

  it('returns error on invalid credentials', async () => {
    const { authService } = require('@/services/supabase/auth.service');
    (authService.login as jest.Mock).mockResolvedValue({
      user: null,
      error: { message: 'Invalid login credentials', status: 401 },
    });

    const request = createMocks({
      method: 'POST',
      body: {
        email: 'test@test.com',
        password: 'wrong-password',
      },
    });

    const response = await POST(request);
    const data = await response.json();

    expect(response.status).toBe(401);
    expect(data.error).toBeDefined();
  });

  it('validates request body', async () => {
    const request = createMocks({
      method: 'POST',
      body: {}, // Missing email and password
    });

    const response = await POST(request);
    const data = await response.json();

    expect(response.status).toBe(400);
    expect(data.error).toBeDefined();
  });
});
```

### 测试服务层

#### 测试AuthService

创建 `tests/services/auth.service.test.ts`:

```typescript
import { AuthService } from '@/services/supabase/auth.service';

// Mock Supabase client
jest.mock('@/lib/supabase/client', () => ({
  supabase: {
    auth: {
      signUp: jest.fn(),
      signInWithPassword: jest.fn(),
      signOut: jest.fn(),
      getUser: jest.fn(),
    },
  },
}));

describe('AuthService', () => {
  let authService: AuthService;

  beforeEach(() => {
    authService = new AuthService();
    jest.clearAllMocks();
  });

  describe('register', () => {
    it('should register a new user successfully', async () => {
      const { supabase } = require('@/lib/supabase/client');
      const mockUser = {
        id: '123',
        email: 'new@test.com',
        user_metadata: { nickname: 'newuser' },
      };

      (supabase.auth.signUp as jest.Mock).mockResolvedValue({
        data: { user: mockUser },
        error: null,
      });

      const result = await authService.register({
        email: 'new@test.com',
        password: 'password123',
        nickname: 'newuser',
      });

      expect(result.user).toEqual(mockUser);
      expect(result.error).toBeNull();
      expect(supabase.auth.signUp).toHaveBeenCalledWith({
        email: 'new@test.com',
        password: 'password123',
        options: {
          data: { nickname: 'newuser' },
        },
      });
    });

    it('should handle registration errors', async () => {
      const { supabase } = require('@/lib/supabase/client');

      (supabase.auth.signUp as jest.Mock).mockResolvedValue({
        data: { user: null },
        error: { message: 'Email already exists' },
      });

      const result = await authService.register({
        email: 'existing@test.com',
        password: 'password123',
      });

      expect(result.user).toBeNull();
      expect(result.error).toBeDefined();
      expect(result.error.message).toBe('Email already exists');
    });

    it('should generate default nickname from email', async () => {
      const { supabase } = require('@/lib/supabase/client');

      (supabase.auth.signUp as jest.Mock).mockResolvedValue({
        data: { user: { id: '123' } },
        error: null,
      });

      await authService.register({
        email: 'test@test.com',
        password: 'password123',
      });

      expect(supabase.auth.signUp).toHaveBeenCalledWith({
        email: 'test@test.com',
        password: 'password123',
        options: {
          data: { nickname: 'test' },
        },
      });
    });
  });

  describe('login', () => {
    it('should login successfully with valid credentials', async () => {
      const { supabase } = require('@/lib/supabase/client');
      const mockUser = {
        id: '123',
        email: 'test@test.com',
      };

      (supabase.auth.signInWithPassword as jest.Mock).mockResolvedValue({
        data: { user: mockUser },
        error: null,
      });

      const result = await authService.login({
        email: 'test@test.com',
        password: 'password123',
      });

      expect(result.user).toEqual(mockUser);
      expect(result.error).toBeNull();
    });

    it('should fail with invalid credentials', async () => {
      const { supabase } = require('@/lib/supabase/client');

      (supabase.auth.signInWithPassword as jest.Mock).mockResolvedValue({
        data: { user: null },
        error: { message: 'Invalid login credentials' },
      });

      const result = await authService.login({
        email: 'test@test.com',
        password: 'wrong-password',
      });

      expect(result.user).toBeNull();
      expect(result.error).toBeDefined();
    });
  });

  describe('logout', () => {
    it('should logout successfully', async () => {
      const { supabase } = require('@/lib/supabase/client');

      (supabase.auth.signOut as jest.Mock).mockResolvedValue({
        error: null,
      });

      const result = await authService.logout();

      expect(result.error).toBeNull();
      expect(supabase.auth.signOut).toHaveBeenCalled();
    });
  });
});
```

#### 测试EvaluationService（AI评估服务）

创建 `tests/services/eval.service.test.ts`:

```typescript
import { EvaluationService, evaluateSpeech } from '@/services/processing/eval.service';
import type { EvaluationInput } from '@/services/processing/eval.service';

// Mock fetch for API calls
global.fetch = jest.fn();

describe('EvaluationService', () => {
  let evaluationService: EvaluationService;

  beforeEach(() => {
    evaluationService = new EvaluationService();
    jest.clearAllMocks();
  });

  describe('evaluate with OpenAI', () => {
    beforeEach(() => {
      process.env.OPENAI_API_KEY = 'sk-test-openai-key';
      delete process.env.ANTHROPIC_API_KEY;
      delete process.env.ZHIPU_API_KEY;
    });

    it('should evaluate speech with OpenAI GPT-4', async () => {
      const mockResponse = {
        choices: [{
          message: {
            content: JSON.stringify({
              overall_score: 85,
              accuracy_score: 80,
              fluency_score: 85,
              pronunciation_score: 88,
              intonation_score: 82,
              feedback: '良い発音です。',
              suggestions: ['もう少し練習を続けましょう。'],
              strengths: ['はっきりとした発音'],
              improvements: ['イントネーションに注意してください']
            })
          }
        }]
      };

      (global.fetch as jest.Mock).mockResolvedValue({
        ok: true,
        json: async () => mockResponse
      });

      const input: EvaluationInput = {
        target_text: 'こんにちは',
        user_transcript: 'こんにちは',
        user_level: 'beginner'
      };

      const result = await evaluationService.evaluate(input, 'openai');

      expect(result.overall_score).toBe(85);
      expect(result.feedback).toBeDefined();
      expect(global.fetch).toHaveBeenCalledWith(
        'https://api.openai.com/v1/chat/completions',
        expect.any(Object)
      );
    });

    it('should handle OpenAI API errors', async () => {
      (global.fetch as jest.Mock).mockResolvedValue({
        ok: false,
        statusText: 'Unauthorized'
      });

      const input: EvaluationInput = {
        target_text: 'こんにちは',
        user_transcript: 'こんにちは'
      };

      const result = await evaluationService.evaluate(input, 'openai');

      // Should fallback to default evaluation
      expect(result).toBeDefined();
      expect(result.overall_score).toBeGreaterThanOrEqual(0);
    });
  });

  describe('evaluate with Anthropic Claude', () => {
    beforeEach(() => {
      delete process.env.OPENAI_API_KEY;
      process.env.ANTHROPIC_API_KEY = 'sk-ant-test-key';
      delete process.env.ZHIPU_API_KEY;
    });

    it('should evaluate speech with Claude', async () => {
      const mockResponse = {
        content: [{
          text: JSON.stringify({
            overall_score: 88,
            accuracy_score: 85,
            fluency_score: 88,
            pronunciation_score: 90,
            intonation_score: 85,
            feedback: '素晴らしい発音です！',
            suggestions: ['引き続き練習してください。'],
            strengths: ['自然なイントネーション'],
            improvements: ['長音の発音を意識してください']
          })
        }]
      };

      (global.fetch as jest.Mock).mockResolvedValue({
        ok: true,
        json: async () => mockResponse
      });

      const input: EvaluationInput = {
        target_text: 'こんにちは',
        user_transcript: 'こんにちは',
        user_level: 'intermediate'
      };

      const result = await evaluationService.evaluate(input, 'anthropic');

      expect(result.overall_score).toBe(88);
      expect(global.fetch).toHaveBeenCalledWith(
        'https://api.anthropic.com/v1/messages',
        expect.any(Object)
      );
    });
  });

  describe('evaluate with Zhipu GLM', () => {
    beforeEach(() => {
      delete process.env.OPENAI_API_KEY;
      delete process.env.ANTHROPIC_API_KEY;
      process.env.ZHIPU_API_KEY = 'test-zhipu-key';
    });

    it('should evaluate speech with GLM-4', async () => {
      const mockResponse = {
        choices: [{
          message: {
            content: JSON.stringify({
              overall_score: 86,
              accuracy_score: 82,
              fluency_score: 86,
              pronunciation_score: 88,
              intonation_score: 84,
              feedback: '発音が很清楚です。',
              suggestions: ['继续练习会更好。'],
              strengths: ['发音标准'],
              improvements: ['注意语调变化']
            })
          }
        }]
      };

      (global.fetch as jest.Mock).mockResolvedValue({
        ok: true,
        json: async () => mockResponse
      });

      const input: EvaluationInput = {
        target_text: 'こんにちは',
        user_transcript: 'こんにちは',
        user_level: 'beginner'
      };

      const result = await evaluationService.evaluate(input, 'zhipu');

      expect(result.overall_score).toBe(86);
      expect(global.fetch).toHaveBeenCalledWith(
        'https://open.bigmodel.cn/api/paas/v4/chat/completions',
        expect.objectContaining({
          method: 'POST',
          headers: expect.objectContaining({
            'Authorization': expect.stringContaining('Bearer')
          })
        })
      );
    });

    it('should handle Zhipu API errors gracefully', async () => {
      (global.fetch as jest.Mock).mockResolvedValue({
        ok: false,
        statusText: 'Invalid API Key'
      });

      const input: EvaluationInput = {
        target_text: 'こんにちは',
        user_transcript: 'こんばんは' // Wrong answer
      };

      const result = await evaluationService.evaluate(input, 'zhipu');

      // Should fallback to default evaluation
      expect(result).toBeDefined();
      expect(result.overall_score).toBeLessThan(100);
    });

    it('should use GLM-4-flash model by default', async () => {
      (global.fetch as jest.Mock).mockResolvedValue({
        ok: true,
        json: async () => ({
          choices: [{
            message: {
              content: JSON.stringify({
                overall_score: 80,
                accuracy_score: 80,
                fluency_score: 80,
                pronunciation_score: 80,
                intonation_score: 80,
                feedback: 'Test feedback',
                suggestions: [],
                strengths: [],
                improvements: []
              })
            }
          }]
        })
      });

      const input: EvaluationInput = {
        target_text: 'こんにちは',
        user_transcript: 'こんにちは'
      };

      await evaluationService.evaluate(input, 'zhipu');

      const fetchCall = (global.fetch as jest.Mock).mock.calls[0];
      const requestBody = JSON.parse(fetchCall[1].body);

      expect(requestBody.model).toBe('glm-4');
    });
  });

  describe('automatic provider selection', () => {
    it('should prioritize OpenAI over others', async () => {
      process.env.OPENAI_API_KEY = 'sk-test-openai';
      process.env.ANTHROPIC_API_KEY = 'sk-ant-test';
      process.env.ZHIPU_API_KEY = 'test-zhipu';

      (global.fetch as jest.Mock).mockResolvedValue({
        ok: true,
        json: async () => ({
          choices: [{
            message: {
              content: JSON.stringify({
                overall_score: 85,
                accuracy_score: 85,
                fluency_score: 85,
                pronunciation_score: 85,
                intonation_score: 85,
                feedback: 'Test',
                suggestions: [],
                strengths: [],
                improvements: []
              })
            }
          }]
        })
      });

      const input: EvaluationInput = {
        target_text: 'こんにちは',
        user_transcript: 'こんにちは'
      };

      const result = await evaluationService.evaluate(input);

      expect(result.overall_score).toBe(85);
      // Should call OpenAI endpoint
      expect(global.fetch).toHaveBeenCalledWith(
        'https://api.openai.com/v1/chat/completions',
        expect.any(Object)
      );
    });

    it('should use Zhipu when only Zhipu key is available', async () => {
      delete process.env.OPENAI_API_KEY;
      delete process.env.ANTHROPIC_API_KEY;
      process.env.ZHIPU_API_KEY = 'test-zhipu';

      (global.fetch as jest.Mock).mockResolvedValue({
        ok: true,
        json: async () => ({
          choices: [{
            message: {
              content: JSON.stringify({
                overall_score: 85,
                accuracy_score: 85,
                fluency_score: 85,
                pronunciation_score: 85,
                intonation_score: 85,
                feedback: 'Test',
                suggestions: [],
                strengths: [],
                improvements: []
              })
            }
          }]
        })
      });

      const input: EvaluationInput = {
        target_text: 'こんにちは',
        user_transcript: 'こんにちは'
      };

      const result = await evaluationService.evaluate(input);

      expect(result.overall_score).toBe(85);
      // Should call Zhipu endpoint
      expect(global.fetch).toHaveBeenCalledWith(
        'https://open.bigmodel.cn/api/paas/v4/chat/completions',
        expect.any(Object)
      );
    });

    it('should use fallback when no API keys are configured', async () => {
      delete process.env.OPENAI_API_KEY;
      delete process.env.ANTHROPIC_API_KEY;
      delete process.env.ZHIPU_API_KEY;

      const input: EvaluationInput = {
        target_text: 'こんにちは',
        user_transcript: 'こんにちは'
      };

      const result = await evaluationService.evaluate(input);

      // Should return default evaluation
      expect(result).toBeDefined();
      expect(result.overall_score).toBeGreaterThanOrEqual(0);
      expect(result.overall_score).toBeLessThanOrEqual(100);
    });
  });
});
```

---

## 端到端测试

### 测试用户注册流程

创建 `tests/e2e/auth/register.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';

test.describe('User Registration', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/register');
  });

  test('displays registration form', async ({ page }) => {
    await expect(page.locator('h1')).toContainText('注册');
    await expect(page.locator('input[type="email"]')).toBeVisible();
    await expect(page.locator('input[type="password"]')).toBeVisible();
    await expect(page.locator('input[type="password"]')).toHaveCount(2);
  });

  test('shows error when passwords do not match', async ({ page }) => {
    await page.fill('input[type="email"]', 'test@example.com');
    await page.fill('input#password', 'password123');
    await page.fill('input#confirmPassword', 'different-password');
    await page.click('button[type="submit"]');

    await expect(page.locator('text=两次输入的密码不一致')).toBeVisible();
  });

  test('shows error when password is too short', async ({ page }) => {
    await page.fill('input[type="email"]', 'test@example.com');
    await page.fill('input#password', '12345');
    await page.fill('input#confirmPassword', '12345');
    await page.click('button[type="submit"]');

    await expect(page.locator('text=密码长度至少为6位')).toBeVisible();
  });

  test('successful registration redirects to dashboard', async ({ page }) => {
    // 使用测试邮箱和时间戳生成唯一邮箱
    const timestamp = Date.now();
    const testEmail = `test${timestamp}@example.com`;

    await page.fill('input[type="email"]', testEmail);
    await page.fill('input#nickname', 'Test User');
    await page.fill('input#password', 'password123');
    await page.fill('input#confirmPassword', 'password123');
    await page.click('button[type="submit"]');

    // 等待导航到仪表板
    await page.waitForURL('/dashboard');
    await expect(page.locator('h1')).toContainText('仪表板');
  });

  test('link to login page works', async ({ page }) => {
    const loginLink = page.locator('text=立即登录');
    await loginLink.click();

    await page.waitForURL('/login');
    await expect(page.locator('h1')).toContainText('登录');
  });
});
```

### 测试用户登录流程

创建 `tests/e2e/auth/login.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';

test.describe('User Login', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/login');
  });

  test('displays login form', async ({ page }) => {
    await expect(page.locator('h1')).toContainText('登录');
    await expect(page.locator('input[type="email"]')).toBeVisible();
    await expect(page.locator('input[type="password"]')).toBeVisible();
    await expect(page.locator('button[type="submit"]')).toBeVisible();
  });

  test('shows error with invalid credentials', async ({ page }) => {
    await page.fill('input[type="email"]', 'wrong@example.com');
    await page.fill('input[type="password"]', 'wrongpassword');
    await page.click('button[type="submit"]');

    // 应该显示错误提示
    await expect(page.locator('.text-red-600')).toBeVisible();
  });

  test('successful login redirects to dashboard', async ({ page }) => {
    // 假设已经注册了测试用户
    await page.fill('input[type="email"]', 'test@example.com');
    await page.fill('input[type="password"]', 'password123');
    await page.click('button[type="submit"]');

    // 等待导航
    await page.waitForURL('/', { timeout: 5000 });
  });

  test('link to register page works', async ({ page }) => {
    const registerLink = page.locator('text=立即注册');
    await registerLink.click();

    await page.waitForURL('/register');
    await expect(page.locator('h1')).toContainText('注册');
  });
});
```

### 测试课程模块

创建 `tests/e2e/courses/courses.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';

test.describe('Courses', () => {
  test.beforeEach(async ({ page }) => {
    // 先登录
    await page.goto('/login');
    await page.fill('input[type="email"]', 'test@example.com');
    await page.fill('input[type="password"]', 'password123');
    await page.click('button[type="submit"]');
    await page.waitForURL('/');
  });

  test('displays course list', async ({ page }) => {
    await page.goto('/courses');

    // 检查课程列表是否存在
    const courseCards = page.locator('[data-testid="course-card"]');
    await expect(courseCards).toHaveCount(5); // 应该有5个测试课程
  });

  test('filters courses by difficulty', async ({ page }) => {
    await page.goto('/courses');

    // 点击N5难度筛选
    await page.click('button:has-text("N5")');

    // 验证只显示N5课程
    const n5Courses = page.locator('[data-difficulty="N5"]');
    await expect(n5Courses).toHaveCount(5);
  });

  test('searches courses', async ({ page }) => {
    await page.goto('/courses');

    // 输入搜索关键词
    await page.fill('input[placeholder*="搜索"]', '初次见面');

    // 等待搜索结果
    const searchResults = page.locator('[data-testid="course-card"]');
    await expect(searchResults).toHaveCount(1);
  });

  test('opens course detail page', async ({ page }) => {
    await page.goto('/courses');

    // 点击第一个课程
    await page.click('[data-testid="course-card"]:first-child');

    // 验证详情页
    await expect(page.locator('h1')).toContainText('初次见面');
    await expect(page.locator('[data-testid="course-description"]')).toBeVisible();
    await expect(page.locator('[data-testid="character-list"]')).toBeVisible();
  });

  test('shows course progress', async ({ page }) => {
    await page.goto('/courses');

    // 检查进度显示
    const progress = page.locator('[data-testid="course-progress"]');
    await expect(progress.first()).toBeVisible();
  });
});
```

### 测试练习模块

创建 `tests/e2e/practice/practice.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';

test.describe('Practice Module', () => {
  test.beforeEach(async ({ page }) => {
    // 登录
    await page.goto('/login');
    await page.fill('input[type="email"]', 'test@example.com');
    await page.fill('input[type="password"]', 'password123');
    await page.click('button[type="submit"]');
    await page.waitForURL('/');
  });

  test('starts practice session', async ({ page }) => {
    // 进入课程详情页
    await page.goto('/courses/1');

    // 选择角色
    await page.click('[data-testid="character-card"]:first-child');

    // 点击开始练习
    await page.click('button:has-text("开始练习")');

    // 验证进入练习页面
    await expect(page.locator('[data-testid="practice-area"]')).toBeVisible();
    await expect(page.locator('[data-testid="chinese-prompt"]')).toBeVisible();
  });

  test('records audio', async ({ page }) => {
    await page.goto('/practice/1');

    const recordButton = page.locator('[data-testid="record-button"]');

    // 点击开始录音
    await recordButton.click();

    // 验证录音状态
    await expect(page.locator('[data-testid="recording-indicator"]')).toBeVisible();

    // 模拟录音3秒
    await page.waitForTimeout(3000);

    // 停止录音
    await recordButton.click();

    // 验证录音完成
    await expect(page.locator('[data-testid="audio-player"]')).toBeVisible();
  });

  test('plays TTS audio', async ({ page }) => {
    await page.goto('/practice/1');

    const playButton = page.locator('[data-testid="tts-play-button"]');

    // 点击播放
    await playButton.click();

    // 验证播放状态
    await expect(page.locator('[data-testid="playing-indicator"]')).toBeVisible();
  });

  test('displays feedback after recording', async ({ page }) => {
    await page.goto('/practice/1');

    // 录音（已mock）
    await page.click('[data-testid="record-button"]');
    await page.waitForTimeout(3000);
    await page.click('[data-testid="record-button"]');

    // 等待评估完成
    await page.waitForSelector('[data-testid="feedback-display"]', { timeout: 10000 });

    // 验证反馈显示
    await expect(page.locator('[data-testid="score-badge"]')).toBeVisible();
    await expect(page.locator('[data-testid="dimension-scores"]')).toBeVisible();
  });

  test('completes practice session', async ({ page }) => {
    await page.goto('/practice/1');

    // 完成所有句子（假设有2句）
    for (let i = 0; i < 2; i++) {
      await page.click('[data-testid="record-button"]');
      await page.waitForTimeout(3000);
      await page.click('[data-testid="record-button"]');
      await page.waitForTimeout(2000);

      if (i < 1) {
        await page.click('button:has-text("继续")');
      }
    }

    // 验证完成页面
    await expect(page.locator('[data-testid="practice-summary"]')).toBeVisible();
    await expect(page.locator('text=练习完成')).toBeVisible();
  });
});
```

### 运行E2E测试

```bash
# 安装浏览器（首次）
npx playwright install

# 运行所有E2E测试
npm run test:e2e

# 以UI模式运行（推荐）
npm run test:e2e:ui

# 只运行特定测试文件
npx playwright test tests/e2e/auth/login.spec.ts

# 调试模式（显示浏览器窗口）
npx playwright test --debug

# 生成测试报告
npx playwright test --reporter=html
```

---

## 手动测试用例

### 功能测试清单

#### 认证模块

| 用例ID | 测试场景 | 测试步骤 | 预期结果 | 状态 |
|--------|---------|---------|---------|------|
| AUTH-001 | 正常注册 | 1. 输入有效邮箱<br>2. 输入密码（6位以上）<br>3. 确认密码<br>4. 点击注册 | 注册成功，跳转到仪表板 | ⏳ |
| AUTH-002 | 邮箱格式验证 | 1. 输入无效邮箱格式<br>2. 点击注册 | 显示邮箱格式错误提示 | ⏳ |
| AUTH-003 | 密码不匹配 | 1. 输入两个不同的密码<br>2. 点击注册 | 显示密码不匹配错误 | ⏳ |
| AUTH-004 | 密码过短 | 1. 输入5位密码<br>2. 点击注册 | 显示密码过短错误 | ⏳ |
| AUTH-005 | 正常登录 | 1. 输入已注册邮箱<br>2. 输入正确密码<br>3. 点击登录 | 登录成功，跳转仪表板 | ⏳ |
| AUTH-006 | 错误密码 | 1. 输入已注册邮箱<br>2. 输入错误密码<br>3. 点击登录 | 显示登录失败提示 | ⏳ |
| AUTH-007 | 登出 | 1. 点击登出按钮 | 返回首页，清除登录状态 | ⏳ |
| AUTH-008 | 昵称自动生成 | 1. 只输入邮箱和密码<br>2. 不输入昵称 | 自动从邮箱生成昵称 | ⏳ |

#### 课程模块

| 用例ID | 测试场景 | 测试步骤 | 预期结果 | 状态 |
|--------|---------|---------|---------|------|
| COURSE-001 | 显示课程列表 | 1. 进入课程页面 | 显示所有可用课程 | ⏳ |
| COURSE-002 | 课程筛选 | 1. 点击N5筛选<br>2. 点击N4筛选 | 只显示对应难度的课程 | ⏳ |
| COURSE-003 | 课程搜索 | 1. 输入搜索关键词<br>2. 查看结果 | 显示匹配的课程 | ⏳ |
| COURSE-004 | 查看课程详情 | 1. 点击课程卡片 | 显示课程详情页 | ⏳ |
| COURSE-005 | 显示课程进度 | 1. 查看已开始课程 | 显示进度百分比 | ⏳ |
| COURSE-006 | 显示角色列表 | 1. 进入课程详情 | 显示所有角色 | ⏳ |
| COURSE-007 | 选择角色 | 1. 点击角色卡片 | 角色被选中 | ⏳ |

#### 练习模块

| 用例ID | 测试场景 | 测试步骤 | 预期结果 | 状态 |
|--------|---------|---------|---------|------|
| PRACTICE-001 | 进入练习页面 | 1. 从课程详情进入练习 | 显示练习界面 | ⏳ |
| PRACTICE-002 | 显示中文提示 | 1. 查看练习区域 | 显示中文含义 | ⏳ |
| PRACTICE-003 | 点击提示按钮 | 1. 点击"显示答案" | 显示日文答案 | ⏳ |
| PRACTICE-004 | 开始录音 | 1. 点击录音按钮 | 开始录音，显示波形 | ⏳ |
| PRACTICE-005 | 停止录音 | 1. 再次点击录音按钮 | 停止录音 | ⏳ |
| PRACTICE-006 | 录音超时 | 1. 录音超过30秒 | 自动停止录音 | ⏳ |
| PRACTICE-007 | 播放TTS | 1. 点击播放按钮 | 播放标准发音 | ⏳ |
| PRACTICE-008 | 查看评估结果 | 1. 录音完成后等待 | 显示评分和反馈 | ⏳ |
| PRACTICE-009 | 查看等级 | 1. 检查评分徽章 | 显示S/A/B/C/D等级 | ⏳ |
| PRACTICE-010 | 查看维度评分 | 1. 检查各维度分数 | 显示5个维度评分 | ⏳ |
| PRACTICE-011 | 跳过当前句 | 1. 点击跳过按钮 | 进入下一句 | ⏳ |
| PRACTICE-012 | 完成练习 | 1. 完成所有句子 | 显示总结页面 | ⏳ |
| PRACTICE-013 | 重新练习 | 1. 点击重新练习 | 重新开始当前课程 | ⏳ |
| PRACTICE-014 | 练习下一个 | 1. 点击下一个课程 | 进入下一课程练习 | ⏳ |

#### AI评估模块

| 用例ID | 测试场景 | 测试步骤 | 预期结果 | 状态 |
|--------|---------|---------|---------|------|
| AI-001 | OpenAI评估 | 1. 配置OPENAI_API_KEY<br>2. 完成一次练习<br>3. 查看评估结果 | 显示OpenAI的评估结果 | ⏳ |
| AI-002 | Anthropic评估 | 1. 配置ANTHROPIC_API_KEY<br>2. 完成一次练习<br>3. 查看评估结果 | 显示Claude的评估结果 | ⏳ |
| AI-003 | 智谱GLM评估 | 1. 配置ZHIPU_API_KEY<br>2. 完成一次练习<br>3. 查看评估结果 | 显示GLM的评估结果 | ⏳ |
| AI-004 | 智谱GLM测试连通性 | 1. 运行curl测试命令<br>2. 查看返回结果 | 返回正常JSON响应 | ⏳ |
| AI-005 | 自动选择AI提供商 | 1. 同时配置多个API Key<br>2. 完成练习 | 优先使用OpenAI评估 | ⏳ |
| AI-006 | 仅配置智谱GLM | 1. 只配置ZHIPU_API_KEY<br>2. 完成练习 | 使用智谱GLM评估 | ⏳ |
| AI-007 | 无API密钥降级 | 1. 不配置任何AI API Key<br>2. 完成练习 | 使用规则评估返回结果 | ⏳ |
| AI-008 | AI API错误处理 | 1. 配置无效的API Key<br>2. 完成练习 | 自动降级到规则评估 | ⏳ |
| AI-009 | 智谱API超时处理 | 1. 模拟网络超时<br>2. 完成练习 | 显示错误或使用降级评估 | ⏳ |
| AI-010 | 评估分数范围验证 | 1. 完成多次练习<br>2. 检查评分 | 所有评分在0-100之间 | ⏳ |
| AI-011 | 评估反馈语言 | 1. 使用智谱GLM评估<br>2. 查看反馈内容 | 反馈使用中文 | ⏳ |
| AI-012 | 多维度评分展示 | 1. 查看评估详情<br>2. 检查维度分数 | 显示5个维度评分 | ⏳ |
| AI-013 | 智谱GLM成本控制 | 1. 完成100次练习<br>2. 查看token使用量 | 成本在预期范围内 | ⏳ |
| AI-014 | 切换AI提供商 | 1. 从OpenAI切换到智谱GLM<br>2. 完成练习 | 使用智谱GLM评估 | ⏳ |

#### 智谱GLM专项测试

| 用例ID | 测试场景 | 测试步骤 | 预期结果 | 状态 |
|--------|---------|---------|---------|------|
| GLM-001 | 注册智谱账号 | 1. 访问open.bigmodel.cn<br>2. 完成注册和实名认证 | 账号创建成功 | ⏳ |
| GLM-002 | 获取API密钥 | 1. 进入控制台<br>2. 创建API密钥 | 成功获取密钥 | ⏳ |
| GLM-003 | 配置环境变量 | 1. 添加ZHIPU_API_KEY到.env.local<br>2. 重启开发服务器 | 配置生效 | ⏳ |
| GLM-004 | 测试基础调用 | 1. 运行curl测试命令 | 返回正确响应 | ⏳ |
| GLM-005 | GLM-4模型评估 | 1. 使用glm-4模型评估日语 | 评估结果准确 | ⏳ |
| GLM-006 | GLM-3-Turbo模型评估 | 1. 使用glm-3-turbo模型<br>2. 对比评估质量 | 成本更低，质量可接受 | ⏳ |
| GLM-007 | 日语支持验证 | 1. 评估不同难度的日语<br>2. 检查评估准确性 | 能准确评估日语 | ⏳ |
| GLM-008 | 中文反馈质量 | 1. 查看评估反馈 | 反馈清晰、有用 | ⏳ |
| GLM-009 | 响应时间测试 | 1. 完成一次评估<br>2. 记录响应时间 | 响应时间<5秒 | ⏳ |
| GLM-010 | 并发请求测试 | 1. 同时发送5个评估请求 | 所有请求都成功处理 | ⏳ |
| GLM-011 | 免费额度监控 | 1. 查看智谱控制台用量 | 显示正确的token使用量 | ⏳ |
| GLM-012 | 余额不足提示 | 1. 使用完免费额度<br>2. 再次评估 | 显示余额不足或计费提示 | ⏳ |

#### 报告模块

| 用例ID | 测试场景 | 测试步骤 | 预期结果 | 状态 |
|--------|---------|---------|---------|------|
| REPORT-001 | 查看日报告 | 1. 进入报告页面<br>2. 选择日报告 | 显示今日学习数据 | ⏳ |
| REPORT-002 | 查看周报告 | 1. 选择周报告 | 显示本周学习数据 | ⏳ |
| REPORT-003 | 查看月报告 | 1. 选择月报告 | 显示本月学习数据 | ⏳ |
| REPORT-004 | 查看进度图表 | 1. 查看报告底部 | 显示学习曲线图 | ⏳ |

#### 分享模块

| 用例ID | 测试场景 | 测试步骤 | 预期结果 | 状态 |
|--------|---------|---------|---------|------|
| SHARE-001 | 复制分享链接 | 1. 点击分享按钮<br>2. 点击复制链接 | 链接复制到剪贴板 | ⏳ |
| SHARE-002 | 访问分享页面 | 1. 打开分享链接 | 显示分享内容 | ⏳ |
| SHARE-003 | 分享成绩卡片 | 1. 点击分享按钮<br>2. 查看预览 | 显示成绩卡片 | ⏳ |

### 兼容性测试

#### 浏览器兼容性

| 浏览器 | 版本 | 状态 | 备注 |
|--------|------|------|------|
| Chrome | 90+ | ⏳ | 主要测试浏览器 |
| Firefox | 88+ | ⏳ |  |
| Safari | 14+ | ⏳ | macOS/iOS |
| Edge | 90+ | ⏳ | Chromium内核 |

#### 设备兼容性

| 设备类型 | 分辨率 | 状态 | 备注 |
|---------|--------|------|------|
| PC | 1920x1080 | ⏳ | 主要分辨率 |
| 平板 | 768x1024 | ⏳ | iPad/Android平板 |
| 手机 | 375x667 | ⏳ | iPhone SE |
| 手机 | 414x896 | ⏳ | iPhone 11 Pro |

---

## 性能测试

### Lighthouse测试

```bash
# 安装Lighthouse CI
npm install -g @lhci/cli

# 生成Lighthouse报告
npm run build
npm run start

# 在另一个终端
lighthouse http://localhost:3000 --view
```

#### 性能指标

| 指标 | 目标值 | 测试方法 | 当前值 | 状态 |
|------|--------|---------|---------|------|
| 性能得分 | >90 | Lighthouse | - | ⏳ |
| 可访问性得分 | >90 | Lighthouse | - | ⏳ |
| 最佳实践得分 | >90 | Lighthouse | - | ⏳ |
| SEO得分 | >90 | Lighthouse | - | ⏳ |
| FCP (First Contentful Paint) | <2s | Lighthouse | - | ⏳ |
| LCP (Largest Contentful Paint) | <2.5s | Lighthouse | - | ⏳ |
| TTI (Time to Interactive) | <3s | Lighthouse | - | ⏳ |
| CLS (Cumulative Layout Shift) | <0.1 | Lighthouse | - | ⏳ |

### 负载测试

使用Apache Bench或类似工具：

```bash
# 安装Apache Bench
# Windows: 下载 http://www.apachelounge.com/download/win64/
# macOS: brew install httpd
```

#### 测试首页

```bash
# 测试首页性能（100个请求，10个并发）
ab -n 100 -c 10 http://localhost:3000/

# 测试课程列表（50个请求，5个并发）
ab -n 50 -c 5 http://localhost:3000/courses
```

---

## 安全测试

### 安全检查清单

#### 认证安全

- [ ] 密码最少6位
- [ ] 密码不以明文存储
- [ ] 登录失败有次数限制
- [ ] Session超时机制
- [ ] 敏感操作需要重新认证

#### 数据安全

- [ ] SQL注入防护
- [ ] XSS攻击防护
- [ ] CSRF防护
- [ ] 文件上传验证
- [ ] API速率限制

#### API安全

- [ ] 所有API验证身份
- [ ] 敏感数据不在URL中
- [ ] HTTPS传输
- [ ] 错误消息不泄露信息

#### 隐私保护

- [ ] 用户数据加密
- [ ] 遵守GDPR/隐私法规
- [ ] 明确的隐私政策
- [ ] 用户数据删除功能

### 安全测试工具

```bash
# npm audit检查已知漏洞
npm audit

# 运行Snyk（需要付费）
npm install -g snyk
snyk test
```

---

## 测试报告模板

### 每日测试报告

```markdown
# 测试日报 - YYYY-MM-DD

## 测试概览
- 测试执行人: XXX
- 测试日期: YYYY-MM-DD
- 测试环境: 开发/测试/预发布
- 测试版本: v0.x.x

## 测试结果统计
- 总测试用例数: XX
- 执行测试用例数: XX
- 通过: XX
- 失败: XX
- 阻塞: XX
- 跳过: XX
- 通过率: XX%

## 新增功能测试
### [功能名称]
- 测试用例数: XX
- 通过: XX
- 失败: XX
- 缺陷列表:
  - [ ] 缺陷1: 描述
  - [ ] 缺陷2: 描述

## 回归测试
### [模块名称]
- 测试用例数: XX
- 通过: XX
- 失败: XX
- 新发现缺陷:
  - [ ] 缺陷1: 描述

## 性能测试
- Lighthouse得分: XX
- 页面加载时间: XX
- API响应时间: XX

## 风险评估
- 高风险问题: XX个
- 中风险问题: XX个
- 低风险问题: XX个

## 明日计划
- [ ] 计划测试内容
- [ ] 需要修复的问题
```

### 缺陷报告模板

```markdown
# 缺陷报告

## 基本信息
- 缺陷ID: BUG-XXX
- 标题: 简短描述
- 发现人: XXX
- 发现时间: YYYY-MM-DD HH:mm
- 严重级别: P0/P1/P2/P3

## 缺陷详情
### 描述
详细描述缺陷现象

### 复现步骤
1. 步骤1
2. 步骤2
3. 步骤3

### 预期行为
描述应该发生的正确行为

### 实际行为
描述实际发生的错误行为

### 环境
- 操作系统: Windows/macOS/Linux
- 浏览器: Chrome/Firefox/Safari
- 版本: XX.X.X
- 设备: PC/平板/手机

### 附件
- 截图: (如果有)
- 录屏: (如果有)

## 严重级别说明
- **P0**: 阻塞性缺陷，无法继续测试或使用
- **P1**: 严重缺陷，主要功能无法使用
- **P2**: 中等缺陷，影响次要功能
- **P3**: 轻微缺陷，不影响使用

## 分配
- 分配给: XXX
- 优先级: 高/中/低
- 目标修复日期: YYYY-MM-DD

## 跟踪
- 状态: 新建/已分配/已修复/待验证/已关闭
- 验证人: XXX
- 验证结果: 通过/失败
- 修复版本: v0.x.x
```

---

## 测试最佳实践

### 单元测试最佳实践

```typescript
// ✅ 好的做法
describe('AuthService', () => {
  beforeEach(() => {
    // 每个测试前重置mock
    jest.clearAllMocks();
  });

  it('does something correctly', () => {
    // Arrange
    const input = 'test';

    // Act
    const result = functionUnderTest(input);

    // Assert
    expect(result).toBe('expected');
  });
});

// ❌ 避免的做法
it('tests too many things', () => {
  // 一个测试验证多个功能
  // 应该拆分成多个测试
});
```

### E2E测试最佳实践

```typescript
// ✅ 好的做法：使用data-testid
await page.click('[data-testid="submit-button"]');

// ❌ 避免的做法：使用CSS选择器（容易变动）
await page.click('.btn-primary');

// ✅ 好的做法：等待元素
await page.waitForSelector('[data-testid="result"]');

// ❌ 避免的做法：固定等待时间
await page.waitForTimeout(3000);
```

### Mock数据管理

创建 `tests/mocks/data.ts`:

```typescript
export const mockUser = {
  id: '123',
  email: 'test@example.com',
  nickname: 'Test User',
};

export const mockCourse = {
  id: 1,
  course_number: 1,
  title_cn: '初次见面',
  title_jp: '初めまして',
  difficulty: 'N5' as const,
  theme: '日常',
};

export const mockSentence = {
  id: 1,
  text_jp: 'こんにちは',
  text_cn: '你好',
  character_id: 1,
};
```

### 测试数据清理

创建测试清理脚本：

```typescript
// tests/setup.ts
export async function cleanupTestData() {
  // 清理测试用户
  await supabase.from('users').delete().eq('email', 'test%@example.com');

  // 清理测试数据
  await supabase.from('practice_records').delete().eq('user_id', 'test-user-id');
}

// 在测试文件中使用
afterAll(async () => {
  await cleanupTestData();
});
```

---

## 持续集成测试

### GitHub Actions配置

创建 `.github/workflows/test.yml`:

```yaml
name: Test

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

      - name: Run TypeScript check
        run: npm run type-check

      - name: Run unit tests
        run: npm run test:coverage

      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage/lcov.info

      - name: Install Playwright
        run: npx playwright install --with-deps

      - name: Run E2E tests
        run: npm run test:e2e

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: playwright-report
          path: playwright-report/
```

---

## 测试总结检查表

在发布前，请确认以下测试项：

### 功能测试
- [ ] 所有用户故事已测试
- [ ] 所有核心功能已验证
- [ ] 边界情况已覆盖
- [ ] 错误处理已验证

### 浏览器测试
- [ ] Chrome最新版本
- [ ] Firefox最新版本
- [ ] Safari最新版本 (macOS/iOS)
- [ ] Edge最新版本
- [ ] 移动浏览器 (Chrome Mobile/Safari Mobile)

### 设备测试
- [ ] PC (1920x1080)
- [ ] 笔记本 (1366x768)
- [ ] 平板 (iPad)
- [ ] 手机 (iPhone SE)
- [ ] 手机 (Android)

### 性能测试
- [ ] Lighthouse得分 >90
- [ ] 页面加载时间 <3s
- [ ] API响应时间 <500ms
- [ ] 内存使用正常

### 安全测试
- [ ] 无SQL注入漏洞
- [ ] 无XSS漏洞
- [ ] 无敏感数据泄露
- [ ] API身份验证正常

### AI服务测试
- [ ] OpenAI API集成测试通过
- [ ] Anthropic Claude API集成测试通过
- [ ] 智谱GLM API集成测试通过
- [ ] AI评估结果格式正确
- [ ] 多维度评分准确
- [ ] 降级机制正常工作
- [ ] API错误处理正确
- [ ] 评估响应时间可接受
- [ ] Token使用量在预算内
- [ ] 智谱GLM日语评估质量验证

---

**文档版本**: v1.1
**最后更新**: 2025年3月7日
**更新内容**:
- 添加智谱GLM API测试支持
- 新增AI评估服务集成测试
- 新增AI评估模块手动测试用例（14个用例）
- 新增智谱GLM专项测试用例（12个用例）
- 更新测试总结检查表，添加AI服务测试项
