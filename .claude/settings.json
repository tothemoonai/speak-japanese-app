{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -q 'vercel --prod' && git status --short | grep -q '^\\s*M' && git add -A && git commit -m \"$(date +'%Y-%m-%d %H:%M:%S') - Auto-commit before deploy\\n\\nFiles changed:\\n$(git diff --cached --name-only | head -10 | sed 's/^/- /')\\n\\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>\" || true",
            "timeout": 30,
            "statusMessage": "Auto-committing before deploy..."
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(git:*)",
      "Bash(vercel:*)",
      "Bash(npm:*)",
      "Read",
      "Write",
      "Edit"
    ],
    "defaultMode": "default"
  },
  "attribution": {
    "commit": "Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>",
    "pr": "Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
  },
  "includeGitInstructions": true
}
